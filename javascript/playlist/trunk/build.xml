
<!-- projet name is used as a base name for distribution files -->
<project name="playlist" default="min">
    <property file="build.properties" />

	<!-- import javascript-tasks (min, pack, lint, gzip, dist) -->
	<import file="${BUILDER_HOME}/js.xml"/>

</project>
